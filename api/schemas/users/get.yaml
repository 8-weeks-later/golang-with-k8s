type: object
properties:
  id:
    type: integer
    format: int32
  email:
    type: string
    format: email
  nickname:
    type: string
  profileImage:
    type: string
  role:
    description: 'required'
    type: string
    format: enum
    enum:
      - ADMIN
      - USER
  createdAt:
    type: string
    format: date-time
  updatedAt:
    type: string
    format: date-time
  mentorProfile:
    $ref: '../mentor_profiles/simpleget.yaml'
